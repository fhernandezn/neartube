{
  "name": "@fhernandezn/neartube",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve-dev": "concurrently -r \"nodemon index.js\" \"cd vue-frontend && npm run serve\"",
    "serve-prod": "npm run build && node index.js",
    "build": "cd vue-frontend && npm run build && rm -rf ../public/dist && cp -r dist ../public/dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "concurrently": "^4.0.1",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "mongoose": "^5.2.6",
    "socket.io": "^2.1.1"
  }
}
